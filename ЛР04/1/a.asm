SD1  SEGMENT BYTE PUBLIC 'DATA'
X    DB   'X'
Y    DB   'Y'
SD1  ENDS

SSEG SEGMENT BYTE STACK 'STACK'
     DB 64 DUP('STACK+++')
SSEG ENDS

SC1  SEGMENT PAGE PUBLIC 'CODE'
     ASSUME CS:SC1,DS:SD1,SS:SSEG
P1   PROC FAR
	MOV AX,SD1
	MOV DS,AX

;	MOV BL,Y
;	MOV CL,X
;	MOV Y,CL
;	MOV X,BL

	XCHG AL,X
	XCHG AL,Y
	XCHG AL,X

	MOV AH,2
	MOV DL,Y
	INT 21H
     
	MOV AX,0B800H
	MOV DS,AX
	MOV BX,3202
	MOV BYTE PTR [BX],'D'
	
	MOV AH,4CH
	INT 21H
P1   ENDP
SC1  ENDS
     END P1
